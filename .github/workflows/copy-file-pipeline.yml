# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      # create a folder named folder1, go to folder1 and create the file named file1.txt with content "This is file1"
      - name: Create folder and file
        run: |
          mkdir folder1
          cd folder1
          echo "This is file1" > file1.txt

      # Runs a set of commands using the runners shell
      # create a folder named folder2, go to folder2 and copy the file named file1.txt from folder1 to folder2
      - name: Create folder2
        run: |
          mkdir folder2
      - name: move file1.txt to folder2
        run: |
          cp folder1/file1.txt folder2/file1.txt    